name: Gitleaks Secret Scan

on:
  push:
    branches: 
      - master
      - feature/test-secrets
  pull_request:
    branches: 
      - master
      - feature/test-secrets

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
